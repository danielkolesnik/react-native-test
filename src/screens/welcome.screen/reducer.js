


const initialState = {
    greetingMessage: 'HELLO ARE YOU READY TO TRASH?!'
};

export default (state = initialState, action) => {
    let {type, ...options} = action;

    switch(type) {

        default:
    }

    return state;
}